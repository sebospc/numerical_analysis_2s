<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/helpColor"
    tools:background="@color/helpColor">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/textView6"
            android:layout_width="match_parent"
            android:layout_height="359dp"
            android:drawableBottom="@drawable/bisection"
            android:gravity="center_horizontal"
            android:text="This method assumes that there is a root in a given interval. That is, there is a continuous function that presents a change of sign. In this interval, the midpoint is determined and reduced to satisfy the tolerance, find a root or exceed the maximum number of iterations."
            android:textColor="@android:color/white"
            android:textSize="19sp"
            tools:layout_editor_absoluteX="8dp"
            tools:layout_editor_absoluteY="3dp"
            tools:textColor="@android:color/white" />

        <TextView
            android:id="@+id/textView35"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
    </LinearLayout>

</android.support.constraint.ConstraintLayout>