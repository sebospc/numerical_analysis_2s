<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/helpColor"
    tools:background="@color/helpColor">

    <TextView
        android:id="@+id/textView5"
        android:layout_width="333dp"
        android:layout_height="282dp"
        android:drawableBottom="@drawable/multiple_roots"
        android:gravity="center_horizontal"
        android:text="This method is a variant of the fixed point method. It is used when the approach to a root has multiplicity m greater than 1. The method recovers the quadratic convergence in the presence of a multiple root."
        android:textColor="@android:color/white"
        android:textSize="19sp"
        tools:layout_editor_absoluteX="26dp"
        tools:layout_editor_absoluteY="16dp"
        tools:textColor="@android:color/white" />
</android.support.constraint.ConstraintLayout>