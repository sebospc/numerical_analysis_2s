<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/helpColor"
    tools:background="@color/helpColor">

    <TextView
        android:id="@+id/textView2"
        android:layout_width="350dp"
        android:layout_height="268dp"
        android:drawableBottom="@drawable/incremental_search"
        android:gravity="center_horizontal"
        android:text="This method assumes a continuous function. The method searches an interval where a root exists. The found interval must comply that exists a change of sign evaluating the function in the extremes of this interval."
        android:textColor="@android:color/white"
        android:textSize="17sp"
        tools:layout_editor_absoluteX="17dp"
        tools:layout_editor_absoluteY="16dp"
        tools:textColor="@android:color/white" />

</android.support.constraint.ConstraintLayout>