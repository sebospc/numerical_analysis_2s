<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/helpColor"
    tools:background="@color/helpColor">

    <TextView
        android:id="@+id/textView2"
        android:layout_width="350dp"
        android:layout_height="290dp"
        android:drawableBottom="@drawable/incremental_search"
        android:gravity="center_horizontal"
        android:text="This method assumes the continuity of the function f(x) in an interval. The method searches an interval where there exist one root of the equation f(x) = 0. For that, there exist a change of sign in the evaluated function in the extremes of found interval."
        android:textColor="@android:color/white"
        android:textSize="17sp"
        tools:layout_editor_absoluteX="17dp"
        tools:layout_editor_absoluteY="16dp"
        tools:textColor="@android:color/white" />

</android.support.constraint.ConstraintLayout>