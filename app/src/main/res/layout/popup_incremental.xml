<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/helpColor"
    tools:background="@color/helpColor">

    <TextView
        android:id="@+id/textView2"
        android:layout_width="350dp"
        android:layout_height="295dp"
        android:drawableBottom="@drawable/incremental_search"
        android:gravity="center_horizontal"
        android:text="This method assumes a continuous function. This looks for an interval where there is a sign change in that function. Start with an initial value and a delta to find intervals until the change of sign is satisfied."
        android:textColor="@android:color/white"
        android:textSize="19sp"
        tools:layout_editor_absoluteX="17dp"
        tools:layout_editor_absoluteY="16dp"
        tools:textColor="@android:color/white" />

</android.support.constraint.ConstraintLayout>